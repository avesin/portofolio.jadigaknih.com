[build.environment]
  NODE_OPTIONS = "--max_old_space_size=4096"

[build]
  command = "pnpm build"
  publish = "dist"

[functions]
  directory = ".netlify/functions-internal/server"
  
[build.processing]
  skip_processing = true

[images]
  remote_images = ["https://images.ctfassets.net/.*"]